<UserControl x:Class="EditableDeletableCollection.EditableDeletablePersonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EditableDeletableCollection"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel>
        <StackPanel Orientation="Horizontal" 
                    Margin="0,10">
            <TextBox Text="{Binding PersonName}" 
                     Width="100"
                     Margin="5">

            </TextBox>
            <TextBox Text="{Binding PersonAge}" 
                     Width="100"
                     Margin="5">

            </TextBox>
            <Button Content="Update" Command="{Binding UpdateButton}"
                    Width="70"
                    Margin="5">

            </Button>
            <Button Content="X" Command="{Binding DeleteButton}"
                    Width="30"
                    Margin="5">
            </Button>
        </StackPanel>
    </StackPanel>
</UserControl>
